# ReUI Base UI Components - Reference

Source: [ReUI v1 docs](https://v1.reui.io/docs/) - Base UI Components (35 components).
Primitives: [@base-ui-components/react](https://base-ui.com/react/overview/quick-start)

## Component Index

| Component | Slug | File |
| --- | --- | --- |
| Base Accordion | [base-accordion](https://v1.reui.io/docs/base-accordion) | `components/ui/base-accordion.tsx` |
| Base Alert Dialog | [base-alert-dialog](https://v1.reui.io/docs/base-alert-dialog) | `components/ui/base-alert-dialog.tsx` |
| Base Autocomplete | [base-autocomplete](https://v1.reui.io/docs/base-autocomplete) | `components/ui/base-autocomplete.tsx` |
| Base Avatar | [base-avatar](https://v1.reui.io/docs/base-avatar) | `components/ui/base-avatar.tsx` |
| Base Badge | [base-badge](https://v1.reui.io/docs/base-badge) | `components/ui/base-badge.tsx` |
| Breadcrumb | [base-breadcrumb](https://v1.reui.io/docs/base-breadcrumb) | `components/ui/base-breadcrumb.tsx` |
| Button | [base-button](https://v1.reui.io/docs/base-button) | `components/ui/button.tsx` |
| Checkbox | [base-checkbox](https://v1.reui.io/docs/base-checkbox) | `components/ui/base-checkbox.tsx` |
| Collapsible | [base-collapsible](https://v1.reui.io/docs/base-collapsible) | `components/ui/base-collapsible.tsx` |
| Base Combobox | [base-combobox](https://v1.reui.io/docs/base-combobox) | `components/ui/base-combobox.tsx` |
| Context Menu | [base-context-menu](https://v1.reui.io/docs/base-context-menu) | `components/ui/base-context-menu.tsx` |
| Base Dialog | [base-dialog](https://v1.reui.io/docs/base-dialog) | `components/ui/base-dialog.tsx` |
| Input | [base-input](https://v1.reui.io/docs/base-input) | `components/ui/base-input.tsx` |
| Base Menu | [base-menu](https://v1.reui.io/docs/base-menu) | `components/ui/base-menu.tsx` |
| Base Menubar | [base-menubar](https://v1.reui.io/docs/base-menubar) | `components/ui/base-menubar.tsx` |
| Meter | [base-meter](https://v1.reui.io/docs/base-meter) | `components/ui/base-meter.tsx` |
| Navigation Menu | [base-navigation-menu](https://v1.reui.io/docs/base-navigation-menu) | `components/ui/base-navigation-menu.tsx` |
| Number Input | [base-number-field](https://v1.reui.io/docs/base-number-field) | `components/ui/base-number-field.tsx` |
| Phone Input | [base-phone-input](https://v1.reui.io/docs/base-phone-input) | `components/ui/base-phone-input.tsx` |
| Base Popover | [base-popover](https://v1.reui.io/docs/base-popover) | `components/ui/base-popover.tsx` |
| Preview Card | [base-preview-card](https://v1.reui.io/docs/base-preview-card) | `components/ui/base-preview-card.tsx` |
| Progress | [base-progress](https://v1.reui.io/docs/base-progress) | `components/ui/base-progress.tsx` |
| Radio Group | [base-radio-group](https://v1.reui.io/docs/base-radio-group) | `components/ui/base-radio-group.tsx` |
| Scroll Area | [base-scroll-area](https://v1.reui.io/docs/base-scroll-area) | `components/ui/base-scroll-area.tsx` |
| Base Select | [base-select](https://v1.reui.io/docs/base-select) | `components/ui/base-select.tsx` |
| Separator | [base-separator](https://v1.reui.io/docs/base-separator) | `components/ui/base-separator.tsx` |
| Sheet | [base-sheet](https://v1.reui.io/docs/base-sheet) | `components/ui/base-sheet.tsx` |
| Slider | [base-slider](https://v1.reui.io/docs/base-slider) | `components/ui/base-slider.tsx` |
| Switch | [base-switch](https://v1.reui.io/docs/base-switch) | `components/ui/base-switch.tsx` |
| Tabs | [base-tabs](https://v1.reui.io/docs/base-tabs) | `components/ui/base-tabs.tsx` |
| Toast | [base-toast](https://v1.reui.io/docs/base-toast) | `components/ui/base-toast.tsx` |
| Toggle | [base-toggle](https://v1.reui.io/docs/base-toggle) | `components/ui/base-toggle.tsx` |
| Toggle Group | [base-toggle-group](https://v1.reui.io/docs/base-toggle-group) | `components/ui/base-toggle-group.tsx` |
| Toolbar | [base-toolbar](https://v1.reui.io/docs/base-toolbar) | `components/ui/base-toolbar.tsx` |
| Base Tooltip | [base-tooltip](https://v1.reui.io/docs/base-tooltip) | `components/ui/base-tooltip.tsx` |

---

## Base Accordion

**Docs:** [https://v1.reui.io/docs/base-accordion](https://v1.reui.io/docs/base-accordion)

A set of collapsible panels with headings, built with Base UI components.

```bash
npm install @base-ui-components/react
```

**Install to:** `components/ui/base-accordion.tsx`

**Examples:** Outline, Solid, Indicator, Nested, Accordion

## API Reference

This component is based on [Base UI Accordion](https://base-ui.com/react/components/accordion).

### Accordion

This component is based on the [Base UI Accordion](https://base-ui.com/react/components/accordion) primitive and includes the following custom props:

| **Prop**    | **Type**                                 | **Default** | **Description**                           |
| ----------- | ---------------------------------------- | ----------- | ----------------------------------------- |
| `variant`   | ~enum~ "default" \| "outline" \| "solid" | ~"default"~ | The visual style variant of the accordion |
| `indicator` | ~enum~ "arrow" \| "plus" \| "none"       | ~"arrow"~   | The type of indicator icon to display     |

---

## Base Alert Dialog

**Docs:** [https://v1.reui.io/docs/base-alert-dialog](https://v1.reui.io/docs/base-alert-dialog)

A dialog that requires user response to proceed, built with Base UI components.

```bash
npm install @base-ui-components/react
```

**Install to:** `components/ui/base-alert-dialog.tsx`

**Examples:** Destructive, Dismiss, No Backdrop

## API Reference

This component is based on the [Base UI Alert Dialog](https://base-ui.com/react/components/alert-dialog) primitive and includes the following custom props:

## Credits

- Built with [Base UI Alert Dialog](https://base-ui.com/react/components/alert-dialog).

---

## Base Autocomplete

**Docs:** [https://v1.reui.io/docs/base-autocomplete](https://v1.reui.io/docs/base-autocomplete)

An input that suggests options as you type. Built on top of Base UI Autocomplete component with shadcn styling.

```bash
npm install @base-ui-components/react
```

**Install to:** `components/ui/base-autocomplete.tsx`

**Examples:** Auto Highlight, Clear Button, Grouped, Async Search, Size, Form

## API Reference

This component is based on [Base UI Autocomplete](https://base-ui.com/react/components/autocomplete).

---

## Base Avatar

**Docs:** [https://v1.reui.io/docs/base-avatar](https://v1.reui.io/docs/base-avatar)

An easily stylable avatar component built with Base UI components.

```bash
npm install @base-ui-components/react
```

**Install to:** `components/ui/base-avatar.tsx`

**Examples:** Fallback, Indicator, Status, Badge, Size, Group, Users, AvatarIndicator, AvatarStatus

## API Reference

This component is based on the [Base UI Avatar](https://base-ui.com/react/components/avatar) primitive.

### AvatarIndicator

This is a custom component used to display an indicator within the avatar.

| **Prop**                                                      | **Type** | **Default** |
| ------------------------------------------------------------- | -------- | ----------- |
| `className` Additional class names for styling the indicator. | ~string~ |             |

### AvatarStatus

This is a custom component for displaying a status indicator on the avatar.

| **Prop**                                                             | **Type**                                         | **Default** |
| -------------------------------------------------------------------- | ------------------------------------------------ | ----------- |
| `variant` Specifies the status of the avatar.                        | ~enum~ "online" \| "offline" \| "busy" \| "away" | `online`    |
| `className` Additional class names for styling the status indicator. | ~string~                                         |             |

---

## Base Badge

**Docs:** [https://v1.reui.io/docs/base-badge](https://v1.reui.io/docs/base-badge)

A customizable badge component built with Base UI and useRender hook for composition.

```bash
npm install @base-ui-components/react
```

**Install to:** `components/ui/base-badge.tsx`

**Examples:** Outline, Light, Ghost, Disabled, Size, Circle, Square, With Icon, With Dot, Remove Button, Render Prop, Badge, BadgeButton, BadgeDot

## API Reference

This component supports both Base UI's `useRender` hook pattern and traditional `asChild` prop for backward compatibility.

### Badge

| **Prop**                                                                                                       | **Type**                                                                                          | **Default** |
| -------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------- | ----------- |
| `render` Element or component to render the badge as.                                                          | ~React.ReactElement~                                                                              | ``          |
| `asChild` Change the default rendered element for the one passed as a child, merging their props and behavior. | ~boolean~                                                                                         | `false`     |
| `variant` The visual style variant of the badge.                                                               | ~enum~ "primary" \| "secondary" \| "success" \| "warning" \| "info" \| "outline" \| "destructive" | `primary`   |
| `appearance` The appearance style of the badge.                                                                | ~enum~ "default" \| "light" \| "outline" \| "ghost"                                               | `default`   |
| `size` The size of the badge.                                                                                  | ~enum~ "xs" \| "sm" \| "md" \| "lg"                                                               | `md`        |
| `shape` The shape of the badge.                                                                                | ~enum~ "default" \| "circle"                                                                      | `default`   |
| `disabled` Whether the badge is disabled.                                                                      | ~boolean~                                                                                         | `false`     |

### BadgeButton

| **Prop**                                                                                                       | **Type**             | **Default** |
| -------------------------------------------------------------------------------------------------------------- | -------------------- | ----------- |
| `render` Element or component to render the badge button as.                                                   | ~React.ReactElement~ | ``          |
| `asChild` Change the default rendered element for the one passed as a child, merging their props and behavior. | ~boolean~            | `false`     |

### BadgeDot

This is a simple span element with no additional props beyond standard HTML attributes.

---

## Breadcrumb

**Docs:** [https://v1.reui.io/docs/base-breadcrumb](https://v1.reui.io/docs/base-breadcrumb)

Displays a hierarchy of links to the current page or resource in an application.

```bash
npm install @base-ui-components/react
```

**Install to:** `components/ui/base-breadcrumb.tsx`

**Examples:** Icon, Separator, Card, Breadcrumb, BreadcrumbList, BreadcrumbItem, BreadcrumbLink, BreadcrumbPage, BreadcrumbSeparator, BreadcrumbEllipsis

## API Reference

This component provides a customizable breadcrumb system for displaying a hierarchy of links.

### Breadcrumb

This component is a flexible container for displaying breadcrumb items.

| **Prop**                                                                 | **Type**          | **Default** |
| ------------------------------------------------------------------------ | ----------------- | ----------- |
| `className` Additional class names for styling the breadcrumb container. | ~string~          |             |
| `separator` The character or element used as a separator.                | ~React.ReactNode~ | ``          |

### BreadcrumbList

This component is used to display a list of breadcrumb items.

| **Prop**                                                            | **Type** | **Default** |
| ------------------------------------------------------------------- | -------- | ----------- |
| `className` Additional class names for styling the breadcrumb list. | ~string~ |             |

### BreadcrumbItem

This component is used to display an individual breadcrumb item.

| **Prop**                                                            | **Type** | **Default** |
| ------------------------------------------------------------------- | -------- | ----------- |
| `className` Additional class names for styling the breadcrumb item. | ~string~ |             |

### BreadcrumbLink

This component is used to display a link within a breadcrumb item.

| **Prop**                                                                   | **Type**  | **Default** |
| -------------------------------------------------------------------------- | --------- | ----------- |
| `className` Additional class names for styling the breadcrumb link.        | ~string~  |             |
| `href` The URL that the breadcrumb link points to.                         | ~string~  | ~#~         |
| `asChild` Renders the link as a child element, using the `Slot` component. | ~boolean~ | `false`     |

### BreadcrumbPage

This component is used to display the current page in the breadcrumb.

| **Prop**                                                            | **Type** | **Default** |
| ------------------------------------------------------------------- | -------- | ----------- |
| `className` Additional class names for styling the breadcrumb page. | ~string~ |             |

### BreadcrumbSeparator

This component is used to display a separator between breadcrumb items.

| **Prop**                                                                 | **Type** | **Default** |
| ------------------------------------------------------------------------ | -------- | ----------- |
| `className` Additional class names for styling the breadcrumb separator. | ~string~ |             |

### BreadcrumbEllipsis

This component is used to display an ellipsis in the breadcrumb.

| **Prop**                                                                | **Type** | **Default** |
| ----------------------------------------------------------------------- | -------- | ----------- |
| `className` Additional class names for styling the breadcrumb ellipsis. | ~string~ |             |

---

## Button

**Docs:** [https://v1.reui.io/docs/base-button](https://v1.reui.io/docs/base-button)

Renders a clickable button or an element styled as a button

```bash
npm install @base-ui-components/react
```

**Install to:** `components/ui/button.tsx`

**Examples:** Secondary, Mono, Destructive, Outline, Dashed, Ghost, With Icon, As Input, Icon Only, Loading, Badge, Size, Radius Full, Link, Full Width, Disabled, Button

## API Reference

This component provides a versatile and customizable button system with various styles, appearances, and sizes.

### Button

This is the root component for creating buttons with multiple variants, appearances, sizes, and shapes.

| **Prop**                                                                    | **Type**                                                                                                                             | **Default** |
| --------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------ | ----------- |
| `variant` Specifies the button's theme.                                     | ~enum~ "primary" \| "mono" \| "destructive" \| "secondary" \| "outline" \| "dashed" \| "ghost" \| "dim" \| "foreground" \| "inverse" | ~primary~   |
| `appearance` Defines the visual style of the button.                        | ~enum~ "ghost"                                                                                                                       |             |
| `size` Determines the size of the button.                                   | ~enum~ "md" \| "sm" \| "xs" \| "xxs"                                                                                                 | ~md~        |
| `shape` Specifies the shape of the button.                                  | ~enum~ "default" \| "circle"                                                                                                         | ~default~   |
| `mode` Defines the button's interaction style.                              | ~enum~ "default" \| "icon" \| "link"                                                                                                 | ~default~   |
| `underline` Sets the underline style for link mode.                         | ~enum~ "solid" \| "dashed"                                                                                                           |             |
| `underlined` Defines a persistent underline for link mode.                  | ~enum~ "solid" \| "dashed"                                                                                                           |             |
| `asChild` Renders the button as a child element using the `Slot` component. | ~boolean~                                                                                                                            | ~false~     |
| `className` Additional class names for styling the button.                  | ~string~                                                                                                                             |             |

---

## Checkbox

**Docs:** [https://v1.reui.io/docs/base-checkbox](https://v1.reui.io/docs/base-checkbox)

A control that allows the user to toggle between checked and not checked.

```bash
npm install @base-ui-components/react
```

**Install to:** `components/ui/base-checkbox.tsx`

**Examples:** Checked, Disabled, Indeterminate, Size, Form, Checkbox

## API Reference

This component is based on the [Base UI Checkbox](https://base-ui.com/react/components/checkbox) primitive.

### Checkbox

A customized checkbox component that extends the Radix UI Checkbox primitive with additional styling and features. It supports all the props from Radix UI's Checkbox.Root component.

| **Prop**                                                               | **Type** | **Default** |
| ---------------------------------------------------------------------- | -------- | ----------- |
| `className` Additional class names for styling the checkbox container. | ~string~ |             |

---

## Collapsible

**Docs:** [https://v1.reui.io/docs/base-collapsible](https://v1.reui.io/docs/base-collapsible)

An interactive component that expands/collapses a panel.

```bash
npm install @base-ui-components/react
```

**Install to:** `components/ui/base-collapsible.tsx`

**Examples:** Card, Collapsible, CollapsibleTrigger, CollapsiblePanel

## API Reference

This component provides a customizable collapsible system with various styles and states.

### Collapsible

This component is based on the [Collapsible.Root](https://www.radix-ui.com/primitives/docs/components/collapsible#root) primitive and includes the following custom props:

| **Prop**                                                                  | **Type** | **Default** |
| ------------------------------------------------------------------------- | -------- | ----------- |
| `className` Additional class names for styling the collapsible container. | ~string~ |             |

### CollapsibleTrigger

This component is based on the [Collapsible.Trigger](https://www.radix-ui.com/primitives/docs/components/collapsible#trigger) primitive.

### CollapsiblePanel

This component is based on the [Collapsible.Panel](https://www.radix-ui.com/primitives/docs/components/collapsible#panel) primitive and includes the following custom props:

| **Prop**                                                                | **Type** | **Default** |
| ----------------------------------------------------------------------- | -------- | ----------- |
| `className` Additional class names for styling the collapsible content. | ~string~ |             |

---

## Base Combobox

**Docs:** [https://v1.reui.io/docs/base-combobox](https://v1.reui.io/docs/base-combobox)

A filterable select input that allows users to choose from predefined options. Built on top of Base UI Combobox component with shadcn styling.

```bash
npm install @base-ui-components/react
```

**Install to:** `components/ui/base-combobox.tsx`

**Examples:** Multi Select, External Tags, Popup Input, Grouped, Creatable, Size, Form

## API Reference

This component is based on [Base UI Combobox](https://base-ui.com/react/components/combobox).

---

## Context Menu

**Docs:** [https://v1.reui.io/docs/base-context-menu](https://v1.reui.io/docs/base-context-menu)

A menu that appears upon user interaction, such as a right-click, providing contextual actions and options.

```bash
npm install @base-ui-components/react
```

**Install to:** `components/ui/base-context-menu.tsx`

**Examples:** API Reference

## API Reference

This component is based on [Base UI Context Menu](https://base-ui.com/react/components/context-menu).

---

## Base Dialog

**Docs:** [https://v1.reui.io/docs/base-dialog](https://v1.reui.io/docs/base-dialog)

A dialog component for displaying content in an overlay, built with Base UI components.

```bash
npm install @base-ui-components/react
```

**Install to:** `components/ui/base-dialog.tsx`

**Examples:** Scroll, No Overlay, Fullscreen

## API Reference

This component is based on the [Base UI Dialog](https://base-ui.com/react/components/dialog) primitive and includes the following custom props:

---

## Input

**Docs:** [https://v1.reui.io/docs/base-input](https://v1.reui.io/docs/base-input)

Displays a form input field, input groups, number input spinners, date input, date time input, time inputs, etc.

```bash
npm install @base-ui-components/react
```

**Install to:** `components/ui/base-input.tsx`

**Examples:** Disabled, Readonly, Addon, Icon, File, Date, Time, Date & Time, Copy to clipboard, Clear button, Size, Form

---

## Base Menu

**Docs:** [https://v1.reui.io/docs/base-menu](https://v1.reui.io/docs/base-menu)

A list of actions in a dropdown, enhanced with keyboard navigation. Built on top of Base UI Menu component with shadcn styling.

```bash
npm install @base-ui-components/react
```

**Install to:** `components/ui/base-menu.tsx`

**Examples:** Nested, Checkbox, Radio Group

## API Reference

This component is based on [Base UI Menu](https://base-ui.com/react/components/menu).

---

## Base Menubar

**Docs:** [https://v1.reui.io/docs/base-menubar](https://v1.reui.io/docs/base-menubar)

A horizontal menu bar with keyboard navigation. Built on top of Base UI Menubar component with shadcn styling.

```bash
npm install @base-ui-components/react
```

**Install to:** `components/ui/base-menubar.tsx`

## API Reference

This component is based on [Base UI Menubar](https://base-ui.com/react/components/menubar).

---

## Meter

**Docs:** [https://v1.reui.io/docs/base-meter](https://v1.reui.io/docs/base-meter)

A meter component for displaying values within a known range, built with Base UI components.

```bash
npm install @base-ui-components/react
```

**Install to:** `components/ui/base-meter.tsx`

## API Reference

This component is based on the [Base UI Meter](https://base-ui.com/react/components/meter) primitive.

---

## Navigation Menu

**Docs:** [https://v1.reui.io/docs/base-navigation-menu](https://v1.reui.io/docs/base-navigation-menu)

A navigation menu component with keyboard navigation and accessibility features. Built on top of Base UI Navigation Menu component.

```bash
npm install @base-ui-components/react
```

**Install to:** `components/ui/base-navigation-menu.tsx`

**Examples:** With Arrow

## API Reference

This component is based on [Base UI Navigation Menu](https://base-ui.com/react/components/navigation-menu) primitive.

---

## Number Input

**Docs:** [https://v1.reui.io/docs/base-number-field](https://v1.reui.io/docs/base-number-field)

A number input component with increment/decrement controls and validation. Built on top of Base UI Number Field component.

```bash
npm install @base-ui-components/react
```

**Install to:** `components/ui/base-number-field.tsx`

**Examples:** Size

## API Reference

This component is based on [Base UI Number Field](https://base-ui.com/react/components/number-field) primitive.

---

## Phone Input

**Docs:** [https://v1.reui.io/docs/base-phone-input](https://v1.reui.io/docs/base-phone-input)

A phone number input component with country selection and validation. Built on top of Base UI components with react-phone-number-input.

```bash
npm install @base-ui-components/react react-phone-number-input
```

**Install to:** `components/ui/base-phone-input.tsx`

**Examples:** Size, Form

## API Reference

This component is built on top of [react-phone-number-input](https://gitlab.com/catamphetamine/react-phone-number-input).

---

## Base Popover

**Docs:** [https://v1.reui.io/docs/base-popover](https://v1.reui.io/docs/base-popover)

A floating panel that appears on demand. Built on top of Base UI Popover component with shadcn styling.

```bash
npm install @base-ui-components/react
```

**Install to:** `components/ui/base-popover.tsx`

**Examples:** Without Arrow, Position

## API Reference

This component is based on [Base UI Popover](https://base-ui.com/react/components/popover).

---

## Preview Card

**Docs:** [https://v1.reui.io/docs/base-preview-card](https://v1.reui.io/docs/base-preview-card)

A preview card component for displaying content with hover effects and interactive states. Built on top of Base UI Preview Card component.

```bash
npm install @base-ui-components/react
```

**Install to:** `components/ui/base-preview-card.tsx`

## API Reference

This component is based on [Base UI Preview Card](https://base-ui.com/react/components/preview-card) primitive.

---

## Progress

**Docs:** [https://v1.reui.io/docs/base-progress](https://v1.reui.io/docs/base-progress)

A progress bar component for displaying task completion status. Built on top of Base UI Progress component.

```bash
npm install @base-ui-components/react
```

**Install to:** `components/ui/base-progress.tsx`

**Examples:** Status, Sizes, Colors, Animated

## API Reference

This component is based on [Base UI Progress](https://base-ui.com/react/components/progress) primitive and includes the following custom props:

#### Progress

The root component that provides context for all progress parts.

| Prop        | Type        | Default | Description                           |
| ----------- | ----------- | ------- | ------------------------------------- |
| `className` | `string`    | -       | Additional CSS classes                |
| `value`     | `number`    | -       | The current progress value (0-100)    |
| `max`       | `number`    | `100`   | The maximum progress value            |
| `children`  | `ReactNode` | -       | The content of the progress component |

#### ProgressValue

The value display component for showing progress percentage.

| Prop        | Type     | Default | Description            |
| ----------- | -------- | ------- | ---------------------- |
| `className` | `string` | -       | Additional CSS classes |

---

## Radio Group

**Docs:** [https://v1.reui.io/docs/base-radio-group](https://v1.reui.io/docs/base-radio-group)

A set of checkable buttons—known as radio buttons—where no more than one of the buttons can be checked at a time.

```bash
npm install @base-ui-components/react
```

**Install to:** `components/ui/base-radio-group.tsx`

**Examples:** Checked, Disabled, Size, Form, RadioGroup, RadioGroupItem

## API Reference

This component is based on the [Base UI Radio Group](https://base-ui.com/react/components/radio-group) primitive.

### RadioGroup

A customized radio group component that extends the Base UI RadioGroup primitive with additional styling and features. It supports all the props from Base UI's RadioGroup component.

| **Prop**    | **Type**                    | **Default** | **Description**                                               |
| ----------- | --------------------------- | ----------- | ------------------------------------------------------------- |
| `className` | ~string~                    |             | Additional class names for styling the radio group container. |
| `size`      | ~enum~ "sm" \| "md" \| "lg" | "md"        | The size of the radio group items.                            |

### RadioGroupItem

A customized radio group item component that extends the Base UI Radio primitive with additional styling and features. It supports all the props from Base UI's Radio.Root component.

| **Prop**    | **Type**                    | **Default** | **Description**                                                              |
| ----------- | --------------------------- | ----------- | ---------------------------------------------------------------------------- |
| `className` | ~string~                    |             | Additional class names for styling the radio group item.                     |
| `size`      | ~enum~ "sm" \| "md" \| "lg" |             | The size of the radio group item. Inherits from RadioGroup if not specified. |
| `value`     | ~string~                    |             | The value of the radio group item.                                           |
| `disabled`  | ~boolean~                   | false       | Whether the radio group item is disabled.                                    |

---

## Scroll Area

**Docs:** [https://v1.reui.io/docs/base-scroll-area](https://v1.reui.io/docs/base-scroll-area)

A component that provides a scrollable area with custom scrollbars, built with Base UI components.

```bash
npm install @base-ui-components/react
```

**Install to:** `components/ui/base-scroll-area.tsx`

**Examples:** ScrollArea, ScrollBar

## API Reference

This component is built using [Base UI Scroll Area](https://base-ui.com/react/components/scroll-area) primitives. For detailed information, please visit the full API reference.

### ScrollArea

This component is based on the [Base UI ScrollArea](https://base-ui.com/react/components/scroll-area) primitive and includes the following custom props:

| **Prop**      | **Type**                                    | **Default** | **Description**                                       |
| ------------- | ------------------------------------------- | ----------- | ----------------------------------------------------- |
| `className`   | ~string~                                    |             | Additional CSS classes for the scroll area container. |
| `orientation` | ~enum~ "horizontal" \| "vertical" \| "both" | "vertical"  | The orientation of the scroll area.                   |

### ScrollBar

A custom scrollbar component that appears when content overflows.

| **Prop**      | **Type**                          | **Default** | **Description**                           |
| ------------- | --------------------------------- | ----------- | ----------------------------------------- |
| `className`   | ~string~                          |             | Additional CSS classes for the scrollbar. |
| `orientation` | ~enum~ "horizontal" \| "vertical" |             | The orientation of the scrollbar.         |

---

## Base Select

**Docs:** [https://v1.reui.io/docs/base-select](https://v1.reui.io/docs/base-select)

A select input that allows users to choose from predefined options. Built on top of Base UI Select component with shadcn styling.

```bash
npm install @base-ui-components/react
```

**Install to:** `components/ui/base-select.tsx`

**Examples:** Indicator Position, Clear, Group, Multiple, Size, Disabled, Disabled Option, Status, Icon, Custom Indicator, Avatar, Badge, Scroll, Form

## API Reference

This component is based on [Base UI Select](https://base-ui.com/react/components/select).

---

## Separator

**Docs:** [https://v1.reui.io/docs/base-separator](https://v1.reui.io/docs/base-separator)

Visually or semantically separates content into groups.

```bash
npm install @base-ui-components/react
```

**Install to:** `components/ui/base-separator.tsx`

## API Reference

This component is based on [Base UI Separator](https://base-ui.com/react/components/separator).

---

## Sheet

**Docs:** [https://v1.reui.io/docs/base-sheet](https://v1.reui.io/docs/base-sheet)

A slide-out panel component that displays content from the side of the screen, built with Base UI components.

```bash
npm install @base-ui-components/react
```

**Install to:** `components/ui/base-sheet.tsx`

**Examples:** Side, Scroll

## API Reference

This component is built using [Base UI Dialog](https://base-ui.com/react/components/dialog) primitives.

---

## Slider

**Docs:** [https://v1.reui.io/docs/base-slider](https://v1.reui.io/docs/base-slider)

A slider component for selecting values within a range. Built on top of Base UI Slider component.

```bash
npm install @base-ui-components/react
```

**Install to:** `components/ui/base-slider.tsx`

**Examples:** Range, Tooltip, Ticks, Labels, Rating, Input, Form, Vertical

## API Reference

This component is based on [Base UI Slider](https://base-ui.com/react/components/slider) primitive and includes the following custom props:

#### Slider

The root component that provides context for all slider parts.

| Prop            | Type                        | Default | Description                         |
| --------------- | --------------------------- | ------- | ----------------------------------- |
| `className`     | `string`                    | -       | Additional CSS classes              |
| `value`         | `number[]`                  | -       | The current slider values           |
| `defaultValue`  | `number[]`                  | -       | The default slider values           |
| `min`           | `number`                    | `0`     | The minimum value                   |
| `max`           | `number`                    | `100`   | The maximum value                   |
| `step`          | `number`                    | `1`     | The step increment                  |
| `onValueChange` | `(value: number[]) => void` | -       | Callback when value changes         |
| `children`      | `ReactNode`                 | -       | The content of the slider component |

#### SliderThumb

The thumb component that can be dragged to change values.

| Prop        | Type     | Default | Description            |
| ----------- | -------- | ------- | ---------------------- |
| `className` | `string` | -       | Additional CSS classes |

---

## Switch

**Docs:** [https://v1.reui.io/docs/base-switch](https://v1.reui.io/docs/base-switch)

A control that allows the user to toggle between checked and unchecked states.

```bash
npm install @base-ui-components/react
```

**Install to:** `components/ui/base-switch.tsx`

**Examples:** Checked, Disabled, Size, Icon, Form

## API Reference

This component is built using [Base UI Switch](https://base-ui.com/react/components/switch) primitives.

---

## Tabs

**Docs:** [https://v1.reui.io/docs/base-tabs](https://v1.reui.io/docs/base-tabs)

A set of layered sections of content—known as tab panels—that are displayed one at a time. Built on top of Base UI Tabs component.

**Install to:** `components/ui/base-tabs.tsx`

**Examples:** Default, Button Variant, Line Variant, Pill Shape, With Icons, With Badges, Vertical, Sizes, Disabled, Tabs, TabsList, TabsTrigger, TabsContent

## API Reference

This component is built using [Base UI Tabs](https://base-ui.com/react/components/tabs) primitives. For detailed information, please visit the full API reference.

### Tabs

The root component that provides context for all tab parts.

| Prop            | Type                         | Default        | Description                            |
| --------------- | ---------------------------- | -------------- | -------------------------------------- |
| `className`     | `string`                     | -              | Additional CSS classes                 |
| `value`         | `string`                     | -              | The controlled value of the active tab |
| `defaultValue`  | `string`                     | -              | The uncontrolled default value         |
| `onValueChange` | `(value: string) => void`    | -              | Callback when active tab changes       |
| `orientation`   | `'horizontal' \| 'vertical'` | `'horizontal'` | The orientation of the tabs            |
| `children`      | `ReactNode`                  | -              | The content of the tabs component      |

### TabsList

The container for tab triggers.

| Prop        | Type                              | Default     | Description                         |
| ----------- | --------------------------------- | ----------- | ----------------------------------- |
| `className` | `string`                          | -           | Additional CSS classes              |
| `variant`   | `'default' \| 'button' \| 'line'` | `'default'` | The visual variant of the tabs list |
| `shape`     | `'default' \| 'pill'`             | `'default'` | The shape of the tabs list          |
| `size`      | `'xs' \| 'sm' \| 'md' \| 'lg'`    | `'md'`      | The size of the tabs                |
| `children`  | `ReactNode`                       | -           | The content of the tabs list        |

### TabsTrigger

The clickable element that activates a tab.

| Prop        | Type        | Default | Description                                |
| ----------- | ----------- | ------- | ------------------------------------------ |
| `className` | `string`    | -       | Additional CSS classes                     |
| `value`     | `string`    | -       | The value of the tab this trigger controls |
| `disabled`  | `boolean`   | `false` | Whether the tab trigger is disabled        |
| `children`  | `ReactNode` | -       | The content of the tab trigger             |

### TabsContent

The content panel for a tab.

| Prop        | Type        | Default | Description                                  |
| ----------- | ----------- | ------- | -------------------------------------------- |
| `className` | `string`    | -       | Additional CSS classes                       |
| `value`     | `string`    | -       | The value of the tab this content belongs to |
| `children`  | `ReactNode` | -       | The content of the tab panel                 |

---

## Toast

**Docs:** [https://v1.reui.io/docs/base-toast](https://v1.reui.io/docs/base-toast)

A brief message that appears at the bottom of the screen to provide feedback about an operation. Built with Base UI Toast component with customizable variants and animations.

```bash
npm install @base-ui-components/react
```

**Install to:** `components/ui/base-toast.tsx`

**Examples:** Positions, Close Button, Type, Custom, Promise

## API Reference

This component is built using [Base UI Toast](https://base-ui.com/react/components/toast) primitives. For detailed information, please visit the full API reference.

---

## Toggle

**Docs:** [https://v1.reui.io/docs/base-toggle](https://v1.reui.io/docs/base-toggle)

A two-state button that can be either on or off.

**Install to:** `components/ui/base-toggle.tsx`

**Examples:** Text, Outline, Size, Toggle Props, Toggle Data Attributes

## API Reference

This component is based on [Base UI Toggle](https://base-ui.com/react/components/toggle).

### Toggle Props

| Prop              | Type                                                                  | Default | Description                                                                                             |
| :---------------- | :-------------------------------------------------------------------- | :------ | :------------------------------------------------------------------------------------------------------ |
| `defaultPressed`  | `boolean`                                                             | `false` | Whether the toggle button is currently pressed. This is the uncontrolled counterpart of `pressed`.      |
| `pressed`         | `boolean`                                                             | -       | Whether the toggle button is currently pressed. This is the controlled counterpart of `defaultPressed`. |
| `onPressedChange` | `(pressed: boolean, eventDetails: Toggle.ChangeEventDetails) => void` | -       | Callback fired when the pressed state is changed.                                                       |
| `disabled`        | `boolean`                                                             | `false` | Whether the component should ignore user interaction.                                                   |
| `className`       | `string \| ((state: Toggle.State) => string)`                         | -       | CSS class applied to the element, or a function that returns a class based on the component's state.    |

### Toggle Data Attributes

| Attribute      | Type | Description                                |
| :------------- | :--- | :----------------------------------------- |
| `data-pressed` | -    | Present when the toggle button is pressed. |

---

## Toggle Group

**Docs:** [https://v1.reui.io/docs/base-toggle-group](https://v1.reui.io/docs/base-toggle-group)

A set of two-state buttons that can be toggled on or off.

**Install to:** `components/ui/base-toggle-group.tsx`

**Examples:** Single, Outline, Size, ToggleGroup Props, ToggleGroup Data Attributes

## API Reference

### ToggleGroup Props

| Prop             | Type                                                                         | Default        | Description                                                                                                                                                                    |
| :--------------- | :--------------------------------------------------------------------------- | :------------- | :----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `defaultValue`   | `any[]`                                                                      | -              | The open state of the toggle group represented by an array of the values of all pressed toggle buttons. This is the uncontrolled counterpart of `value`.                       |
| `value`          | `any[]`                                                                      | -              | The open state of the toggle group represented by an array of the values of all pressed toggle buttons. This is the controlled counterpart of `defaultValue`.                  |
| `onValueChange`  | `(groupValue: any[], eventDetails: Toggle.Group.ChangeEventDetails) => void` | -              | Callback fired when the pressed states of the toggle group changes.                                                                                                            |
| `toggleMultiple` | `boolean`                                                                    | `false`        | When `false` only one item in the group can be pressed. If any item in the group becomes pressed, the others will become unpressed. When `true` multiple items can be pressed. |
| `disabled`       | `boolean`                                                                    | `false`        | Whether the toggle group should ignore user interaction.                                                                                                                       |
| `loop`           | `boolean`                                                                    | `true`         | Whether to loop keyboard focus back to the first item when the end of the list is reached while using the arrow keys.                                                          |
| `orientation`    | `Orientation`                                                                | `'horizontal'` | -                                                                                                                                                                              |
| `className`      | `string \| ((state: Toggle.Group.State) => string)`                          | -              | CSS class applied to the element, or a function that returns a class based on the component's state.                                                                           |

### ToggleGroup Data Attributes

| Attribute          | Type                         | Description                                                                                        |
| :----------------- | :--------------------------- | :------------------------------------------------------------------------------------------------- |
| `data-orientation` | `'horizontal' \| 'vertical'` | Indicates the orientation of the toggle group.                                                     |
| `data-disabled`    | -                            | Present when the toggle group is disabled.                                                         |
| `data-multiple`    | -                            | Present when the toggle group allows multiple buttons to be in the pressed state at the same time. |

---

## Toolbar

**Docs:** [https://v1.reui.io/docs/base-toolbar](https://v1.reui.io/docs/base-toolbar)

A flexible toolbar component for organizing actions and controls. Built on top of Base UI Toolbar component with comprehensive styling.

```bash
npm install @base-ui-components/react/toolbar
```

**Install to:** `components/ui/base-toolbar.tsx`

## API Reference

This component is based on [Base UI Toolbar](https://base-ui.com/react/components/toolbar).

---

## Base Tooltip

**Docs:** [https://v1.reui.io/docs/base-tooltip](https://v1.reui.io/docs/base-tooltip)

A popup that displays information related to an element when the element receives keyboard focus or the mouse hovers over it. Built on top of Base UI Tooltip component with shadcn styling.

```bash
npm install @base-ui-components/react
```

**Install to:** `components/ui/base-tooltip.tsx`

**Examples:** Light, Side, Without Arrow

## API Reference

This component is based on [Base UI Tooltip](https://base-ui.com/react/components/tooltip).

---


